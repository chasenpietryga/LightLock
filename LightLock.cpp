cpp #include LightLock.h extern C { #include xxhash.h // header-only hash } bool hit(uint64_t hash, float color, float weight) { return cache_hit(hash, color, weight); } void store(uint64_t hash, const float color, float weight, bool permanent) { cache_store(hash, color, weight, permanent); } void invalidate_sector(const float min, const float max) { cache_invalidate(min, max); } void flush() { cache_flush(); }
